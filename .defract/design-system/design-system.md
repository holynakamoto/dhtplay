# Design System

## Overview

No styling conventions detected. This project (`dhtplay`) is a Python command-line application that takes a BitTorrent infohash and plays the torrent via WebTorrent and VLC. It has no CSS, design tokens, styling framework, or web UI of any kind.
